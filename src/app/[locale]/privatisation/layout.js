export const metadata = {
    title: "Privatisations",
    description: "Anniversaire ? EVJF ? EVG ? Privatisez notre bateau avec son skipper ! Devis sur simple demande…",
  };
  
  export default function NosEngagementLayout({ children }) {
    return (
      <>
        {children}
      </>
    );
  }
  